# Dependências de execução (produção e desenvolvimento).
# Instale com: pip install -r requirements.txt
Django==5.2.1
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0
django-environ==0.12.0
django-filter==25.1
django-cors-headers==4.7.0
drf-spectacular==0.28.0
# Trilha de auditoria: registra create/update/delete de todos os models com diff
# campo-a-campo e autor (via AuditlogMiddleware). Base da aba "Logs".
django-auditlog==3.0.0
whitenoise==6.9.0
gunicorn==23.0.0
# Manipulação de imagens (obrigatório para ImageField — foto de perfil).
Pillow==11.1.0
# Driver PostgreSQL — usado apenas em produção (em dev o banco é SQLite).
psycopg[binary]==3.2.9
# Armazenamento de mídias no Amazon S3 (bucket PRIVADO + URLs assinadas).
# Ativado só quando USE_S3=True; em dev o storage continua sendo o disco local.
# O extra [s3] já traz o boto3.
django-storages[s3]==1.14.4
