[pytest]
# Em testes usamos o settings de dev (SQLite em memória via pytest-django).
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = tests.py test_*.py *_tests.py
addopts = -ra --strict-markers
testpaths = apps
